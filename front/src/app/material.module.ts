import { NgModule } from '@angular/core';

// Placeholder module: Angular Material was intentionally removed. Keep a small module so
// any legacy imports referencing MaterialModule won't break the compilation.
@NgModule({
    exports: []
})
export class MaterialModule { }
